// STUB — replace internals with real GET /files when backend is ready
export async function fetchFiles(): Promise<
  { id: string; name: string; source: string }[]
> {
  console.log('[stub] fetchFiles called')
  return []
}
