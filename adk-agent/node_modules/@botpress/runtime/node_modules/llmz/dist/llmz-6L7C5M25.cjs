"use strict";Object.defineProperty(exports, "__esModule", {value: true}); function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; } function _nullishCoalesce(lhs, rhsFn) { if (lhs != null) { return lhs; } else { return rhsFn(); } }

var _chunk5CXOW4DCcjs = require('./chunk-5CXOW4DC.cjs');







var _chunkC4CL2QZIcjs = require('./chunk-C4CL2QZI.cjs');
require('./chunk-3SZK4YSR.cjs');
require('./chunk-2JTZOTO5.cjs');
require('./chunk-BLB44ZDU.cjs');










var _chunkPK72FAKDcjs = require('./chunk-PK72FAKD.cjs');


var _chunkKIN7Y247cjs = require('./chunk-KIN7Y247.cjs');
require('./chunk-F4OZHUMS.cjs');
require('./chunk-K2B5YEGJ.cjs');
require('./chunk-54G63644.cjs');


var _chunkUCXQ4U7Pcjs = require('./chunk-UCXQ4U7P.cjs');



var _chunk4VYDQ3VFcjs = require('./chunk-4VYDQ3VF.cjs');





var _chunkRKKRSBX3cjs = require('./chunk-RKKRSBX3.cjs');

// src/llmz.ts
var _client = require('@botpress/client');
var _cognitive = require('@botpress/cognitive');
var _zui = require('@bpinternal/zui');
var _ms = require('ms'); var _ms2 = _interopRequireDefault(_ms);
var _ulid = require('ulid');

// src/abort-signal.ts
function createJoinedAbortController(signals) {
  const controller = new AbortController();
  const validSignals = signals.filter((signal) => signal != null);
  if (validSignals.length === 0) {
    return controller;
  }
  for (const signal of validSignals) {
    if (signal.aborted) {
      controller.abort(signal.reason);
      return controller;
    }
  }
  const abortListeners = [];
  for (const signal of validSignals) {
    const listener = () => {
      controller.abort(signal.reason);
      cleanup();
    };
    signal.addEventListener("abort", listener);
    abortListeners.push(() => signal.removeEventListener("abort", listener));
  }
  const cleanup = () => {
    abortListeners.forEach((removeListener) => removeListener());
  };
  controller.signal.addEventListener("abort", cleanup, { once: true });
  return controller;
}

// src/llmz.ts
var getErrorMessage = (err) => err instanceof Error ? err.message : JSON.stringify(err);
var SLOW_TOOL_WARNING = _ms2.default.call(void 0, "15s");
var RESPONSE_LENGTH_BUFFER = {
  MIN_TOKENS: 1e3,
  MAX_TOKENS: 16e3,
  PERCENTAGE: 0.1
};
var getModelOutputLimit = (inputLength) => _chunkRKKRSBX3cjs.clamp_default.call(void 0, 
  RESPONSE_LENGTH_BUFFER.PERCENTAGE * inputLength,
  RESPONSE_LENGTH_BUFFER.MIN_TOKENS,
  RESPONSE_LENGTH_BUFFER.MAX_TOKENS
);
var executeContext = async (props) => {
  var _a, _b;
  await _chunk4VYDQ3VFcjs.init.call(void 0, );
  const result = await _executeContext(props);
  try {
    (_b = (_a = result.context.chat) == null ? void 0 : _a.onExecutionDone) == null ? void 0 : _b.call(_a, result);
  } catch (e) {
  }
  return result;
};
var _executeContext = async (props) => {
  var _a, _b;
  const controller = createJoinedAbortController([props.signal]);
  const { onIterationStart, onIterationEnd, onTrace, onExit, onBeforeExecution, onAfterTool, onBeforeTool } = props;
  const client = _nullishCoalesce(props.client, () => ( new (0, _client.Client)()));
  const cognitive = _cognitive.Cognitive.isCognitiveClient(client) ? client : new (0, _cognitive.Cognitive)({ client, __experimental_beta: true });
  const cleanups = [];
  const ctx = new (0, _chunkC4CL2QZIcjs.Context)({
    chat: props.chat,
    instructions: props.instructions,
    objects: props.objects,
    tools: props.tools,
    loop: (_a = props.options) == null ? void 0 : _a.loop,
    timeout: (_b = props.options) == null ? void 0 : _b.timeout,
    exits: props.exits,
    snapshot: props.snapshot,
    model: props.model,
    temperature: props.temperature,
    reasoningEffort: props.reasoningEffort
  });
  try {
    while (true) {
      if (ctx.iterations.length >= ctx.loop) {
        return new (0, _chunkC4CL2QZIcjs.ErrorExecutionResult)(ctx, new (0, _chunkPK72FAKDcjs.LoopExceededError)());
      }
      const iteration = await ctx.nextIteration();
      try {
        await executeOnIterationStartHook({
          iteration,
          ctx,
          onIterationStart,
          controller,
          onIterationEnd
        });
      } catch (err) {
        if (err instanceof _chunkPK72FAKDcjs.ThinkSignal) {
          continue;
        }
      }
      if (controller.signal.aborted) {
        iteration.end({
          type: "aborted",
          aborted: {
            reason: _nullishCoalesce(controller.signal.reason, () => ( "The operation was aborted"))
          }
        });
        return new (0, _chunkC4CL2QZIcjs.ErrorExecutionResult)(ctx, _nullishCoalesce(controller.signal.reason, () => ( "The operation was aborted")));
      }
      cleanups.push(
        iteration.traces.onPush((traces) => {
          for (const trace of traces) {
            onTrace == null ? void 0 : onTrace({ trace, iteration: ctx.iterations.length });
          }
        })
      );
      try {
        await executeIteration({
          iteration,
          ctx,
          cognitive,
          controller,
          onExit,
          onBeforeExecution,
          onAfterTool,
          onBeforeTool
        });
      } catch (err) {
        if (err instanceof _chunkPK72FAKDcjs.CognitiveError) {
          return new (0, _chunkC4CL2QZIcjs.ErrorExecutionResult)(ctx, err);
        }
        iteration.end({
          type: "execution_error",
          execution_error: {
            message: "An unexpected error occurred: " + getErrorMessage(err),
            stack: _chunkKIN7Y247cjs.cleanStackTrace.call(void 0, _nullishCoalesce(err.stack, () => ( "No stack trace available")))
          }
        });
      }
      try {
        await (onIterationEnd == null ? void 0 : onIterationEnd(iteration, controller));
      } catch (err) {
        console.error(err);
      }
      if (iteration.status.type === "exit_success") {
        const exitName = iteration.status.exit_success.exit_name;
        return new (0, _chunkC4CL2QZIcjs.SuccessExecutionResult)(ctx, {
          exit: iteration.exits.find((x) => x.name === exitName),
          result: iteration.status.exit_success.return_value
        });
      }
      if (iteration.status.type === "callback_requested") {
        return new (0, _chunkC4CL2QZIcjs.PartialExecutionResult)(
          ctx,
          iteration.status.callback_requested.signal,
          _chunkC4CL2QZIcjs.Snapshot.fromSignal(iteration.status.callback_requested.signal)
        );
      }
      if (iteration.status.type === "thinking_requested" || iteration.status.type === "exit_error" || iteration.status.type === "execution_error" || iteration.status.type === "invalid_code_error") {
        continue;
      }
      return new (0, _chunkC4CL2QZIcjs.ErrorExecutionResult)(ctx, _nullishCoalesce(iteration.error, () => ( `Unknown error. Status: ${iteration.status.type}`)));
    }
  } catch (error) {
    return new (0, _chunkC4CL2QZIcjs.ErrorExecutionResult)(ctx, _nullishCoalesce(error, () => ( "Unknown error")));
  } finally {
    for (const cleanup of cleanups) {
      try {
        cleanup();
      } catch (e2) {
      }
    }
  }
};
var executeIteration = async ({
  iteration,
  ctx,
  cognitive,
  controller,
  onExit,
  onBeforeExecution,
  onBeforeTool,
  onAfterTool
}) => {
  var _a, _b, _c, _d;
  let startedAt = Date.now();
  const traces = iteration.traces;
  const modelRef = Array.isArray(iteration.model) ? iteration.model[0] : iteration.model;
  const model = await cognitive.getModelDetails(modelRef).catch((thrown) => {
    throw new (0, _chunkPK72FAKDcjs.CognitiveError)(`Failed to fetch model details for model "${modelRef}": ${getErrorMessage(thrown)}`);
  });
  const modelLimit = Math.max(model.input.maxTokens, 8e3);
  const responseLengthBuffer = getModelOutputLimit(modelLimit);
  const messages = _chunkUCXQ4U7Pcjs.truncateWrappedContent.call(void 0, {
    messages: iteration.messages,
    tokenLimit: modelLimit - responseLengthBuffer,
    throwOnFailure: true
  }).filter(
    (x) => (
      // Filter out empty messages, as they are not valid inputs for the LLM
      // This can happen when a message is truncated and the content is empty
      typeof x.content !== "string" || x.content.trim().length > 0
    )
  );
  iteration.messages = messages;
  traces.push({
    type: "llm_call_started",
    started_at: startedAt,
    ended_at: startedAt,
    model: model.ref
  });
  const output = await cognitive.generateContent({
    signal: controller.signal,
    systemPrompt: (_a = messages.find((x) => x.role === "system")) == null ? void 0 : _a.content,
    model: model.ref,
    temperature: iteration.temperature,
    responseFormat: "text",
    reasoningEffort: iteration.reasoningEffort,
    messages: messages.filter((x) => x.role !== "system"),
    stopSequences: ctx.version.getStopTokens()
  }).catch((thrown) => {
    throw new (0, _chunkPK72FAKDcjs.CognitiveError)(`LLM generation failed: ${getErrorMessage(thrown)}`);
  });
  const out = typeof ((_c = (_b = output.output.choices) == null ? void 0 : _b[0]) == null ? void 0 : _c.content) === "string" ? output.output.choices[0].content : null;
  if (!out) {
    throw new (0, _chunkPK72FAKDcjs.CognitiveError)("LLM did not return any text output");
  }
  const assistantResponse = ctx.version.parseAssistantResponse(out);
  iteration.code = assistantResponse.code.trim();
  if (typeof onBeforeExecution === "function") {
    try {
      const hookRes = await onBeforeExecution(iteration, controller);
      if (typeof (hookRes == null ? void 0 : hookRes.code) === "string" && hookRes.code.trim().length > 0) {
        iteration.code = hookRes.code.trim();
      }
    } catch (err) {
      if (err instanceof _chunkPK72FAKDcjs.ThinkSignal) {
        return iteration.end({
          type: "thinking_requested",
          thinking_requested: {
            variables: err.context,
            reason: err.reason
          }
        });
      }
      return iteration.end({
        type: "execution_error",
        execution_error: {
          message: `Error in onBeforeExecution hook: ${getErrorMessage(err)}`,
          stack: _chunkKIN7Y247cjs.cleanStackTrace.call(void 0, _nullishCoalesce(err.stack, () => ( "No stack trace available")))
        }
      });
    }
  }
  iteration.llm = {
    cached: output.meta.cached || false,
    ended_at: Date.now(),
    started_at: startedAt,
    status: "success",
    tokens: output.meta.tokens.input + output.meta.tokens.output,
    spend: output.meta.cost.input + output.meta.cost.output,
    output: assistantResponse.raw,
    model: `${output.meta.model.integration}:${output.meta.model.model}`,
    usage: output.output.usage
  };
  traces.push({
    type: "llm_call_success",
    started_at: startedAt,
    ended_at: iteration.llm.ended_at,
    model: model.ref,
    code: iteration.code
  });
  const vmContext = { ..._chunk4VYDQ3VFcjs.stripInvalidIdentifiers.call(void 0, iteration.variables) };
  for (const obj of iteration.objects) {
    const internalValues = {};
    const instance = {};
    for (const { name, value, writable, type } of _nullishCoalesce(obj.properties, () => ( []))) {
      internalValues[name] = value;
      const initialValue = value;
      const schema = _nullishCoalesce(type, () => ( _zui.z.any()));
      Object.defineProperty(instance, name, {
        enumerable: true,
        configurable: true,
        get() {
          return internalValues[name];
        },
        set(value2) {
          if (_chunkRKKRSBX3cjs.isEqual_default.call(void 0, value2, internalValues[name])) {
            return;
          }
          if (!writable) {
            throw new (0, _chunkPK72FAKDcjs.AssignmentError)(`Property ${obj.name}.${name} is read-only and cannot be modified`);
          }
          if (value2 === internalValues[name]) {
            return;
          }
          const parsed = schema.safeParse(value2);
          if (!parsed.success) {
            throw new (0, _chunkPK72FAKDcjs.AssignmentError)(
              `Invalid value for Object property ${obj.name}.${name}: ${getErrorMessage(parsed.error)}`
            );
          }
          internalValues[name] = parsed.data;
          traces.push({
            type: "property",
            started_at: Date.now(),
            object: obj.name,
            property: name,
            value: parsed.data
          });
          iteration.trackMutation({ object: obj.name, property: name, before: initialValue, after: parsed.data });
        }
      });
    }
    for (const tool of _nullishCoalesce(obj.tools, () => ( []))) {
      instance[tool.name] = wrapTool({
        tool,
        traces,
        object: obj.name,
        iteration,
        beforeHook: onBeforeTool,
        afterHook: onAfterTool,
        controller
      });
    }
    Object.preventExtensions(instance);
    Object.seal(instance);
    vmContext[obj.name] = instance;
  }
  for (const tool of iteration.tools) {
    const wrapped = wrapTool({ tool, traces, iteration, beforeHook: onBeforeTool, afterHook: onAfterTool, controller });
    for (const key of [tool.name, ..._nullishCoalesce(tool.aliases, () => ( []))]) {
      vmContext[key] = wrapped;
    }
  }
  if (controller.signal.aborted) {
    traces.push({
      type: "abort_signal",
      started_at: Date.now(),
      reason: "The operation was aborted by user."
    });
    return iteration.end({
      type: "aborted",
      aborted: {
        reason: _nullishCoalesce(controller.signal.reason, () => ( "The operation was aborted"))
      }
    });
  }
  startedAt = Date.now();
  const result = await _chunk5CXOW4DCcjs.runAsyncFunction.call(void 0, 
    vmContext,
    iteration.code,
    traces,
    controller.signal,
    ctx.timeout
  ).catch((err) => {
    return {
      success: false,
      error: err,
      lines_executed: [],
      traces: [],
      variables: {}
    };
  });
  if (result.error && result.error instanceof _chunkPK72FAKDcjs.InvalidCodeError) {
    return iteration.end({
      type: "invalid_code_error",
      invalid_code_error: {
        message: result.error.message
      }
    });
  }
  traces.push({
    type: "code_execution",
    lines_executed: _nullishCoalesce(result.lines_executed, () => ( 0)),
    started_at: startedAt,
    ended_at: Date.now()
  });
  if (controller.signal.aborted) {
    return iteration.end({
      type: "aborted",
      aborted: {
        reason: _nullishCoalesce(controller.signal.reason, () => ( "The operation was aborted"))
      }
    });
  }
  if (result.error && result.error instanceof _chunkPK72FAKDcjs.CodeExecutionError) {
    return iteration.end({
      type: "execution_error",
      execution_error: {
        message: result.error.message,
        stack: _chunkKIN7Y247cjs.cleanStackTrace.call(void 0, _nullishCoalesce(_nullishCoalesce(result.error.stacktrace, () => ( result.error.stack)), () => ( "No stack trace available")))
      }
    });
  }
  if (!result.success) {
    return iteration.end({
      type: "execution_error",
      execution_error: {
        message: _nullishCoalesce(((_d = result == null ? void 0 : result.error) == null ? void 0 : _d.message), () => ( "Unknown error occurred")),
        stack: _chunkKIN7Y247cjs.cleanStackTrace.call(void 0, _nullishCoalesce(result.error.stack, () => ( "No stack trace available")))
      }
    });
  }
  if (result.signal instanceof _chunkPK72FAKDcjs.ThinkSignal) {
    return iteration.end({
      type: "thinking_requested",
      thinking_requested: {
        variables: result.signal.context,
        reason: result.signal.reason,
        metadata: result.signal.metadata
      }
    });
  }
  if (result.signal instanceof _chunkPK72FAKDcjs.SnapshotSignal) {
    return iteration.end({
      type: "callback_requested",
      callback_requested: {
        signal: result.signal
      }
    });
  }
  let returnValue = result.success && result.return_value ? result.return_value : null;
  const returnAction = returnValue == null ? void 0 : returnValue.action;
  if (returnAction === "think") {
    const variables = _chunkRKKRSBX3cjs.omit_default.call(void 0, _nullishCoalesce(returnValue, () => ( {})), "action");
    if (_chunkRKKRSBX3cjs.isPlainObject_default.call(void 0, variables) && Object.keys(variables).length > 0) {
      return iteration.end({
        type: "thinking_requested",
        thinking_requested: {
          variables,
          reason: "Thinking requested"
        }
      });
    }
    return iteration.end({
      type: "thinking_requested",
      thinking_requested: {
        reason: "Thinking requested",
        variables: iteration.variables
      }
    });
  }
  const parsedExit = _chunkC4CL2QZIcjs.parseExit.call(void 0, returnValue, iteration.exits);
  if (!parsedExit.success) {
    return iteration.end({
      type: "exit_error",
      exit_error: {
        exit: _nullishCoalesce((returnValue == null ? void 0 : returnValue.action), () => ( "n/a")),
        message: parsedExit.error,
        return_value: returnValue
      }
    });
  }
  const returnExit = parsedExit.exit;
  returnValue = { action: returnExit.name, value: parsedExit.value };
  try {
    await (onExit == null ? void 0 : onExit({
      exit: returnExit,
      result: returnValue == null ? void 0 : returnValue.value
    }));
  } catch (err) {
    return iteration.end({
      type: "exit_error",
      exit_error: {
        exit: returnExit.name,
        message: `Error executing exit ${returnExit.name}: ${getErrorMessage(err)}`,
        return_value: returnValue
      }
    });
  }
  return iteration.end({
    type: "exit_success",
    exit_success: {
      exit_name: returnExit.name,
      return_value: returnValue == null ? void 0 : returnValue.value
    }
  });
};
function wrapTool({ tool, traces, object, iteration, beforeHook, afterHook, controller }) {
  const getToolInput = (input) => _nullishCoalesce(tool.zInput.safeParse(input).data, () => ( input));
  return function(input) {
    const toolCallId = `tcall_${_ulid.ulid.call(void 0, )}`;
    const alertSlowTool = setTimeout(
      () => traces.push({
        type: "tool_slow",
        tool_name: tool.name,
        tool_call_id: toolCallId,
        started_at: Date.now(),
        input: getToolInput(input),
        object,
        duration: SLOW_TOOL_WARNING
      }),
      SLOW_TOOL_WARNING
    );
    const cancelSlowTool = () => clearTimeout(alertSlowTool);
    const toolStart = Date.now();
    let output;
    let error;
    let success = true;
    const handleSignals = (error2) => {
      if (output === error2) {
        return true;
      }
      if (error2 instanceof _chunkPK72FAKDcjs.SnapshotSignal) {
        error2.toolCall = {
          name: tool.name,
          inputSchema: tool.input,
          outputSchema: tool.output,
          input
        };
        error2.message = _chunkPK72FAKDcjs.Signals.serializeError(error2);
      }
      if (error2 instanceof _chunkPK72FAKDcjs.ThinkSignal) {
        traces.push({
          type: "think_signal",
          started_at: Date.now(),
          line: 0,
          ended_at: Date.now()
        });
        success = true;
        output = error2;
        return true;
      }
      return false;
    };
    try {
      const withHooks = async (input2) => {
        const beforeRes = await (beforeHook == null ? void 0 : beforeHook({
          iteration,
          tool,
          input: input2,
          controller,
          object,
          toolCallId
        }));
        if (typeof (beforeRes == null ? void 0 : beforeRes.input) !== "undefined") {
          input2 = beforeRes.input;
        }
        let output2 = await tool.execute(input2, {
          callId: toolCallId
        });
        const afterRes = await (afterHook == null ? void 0 : afterHook({
          iteration,
          tool,
          input: input2,
          output: output2,
          controller,
          object,
          toolCallId
        }));
        if (typeof (afterRes == null ? void 0 : afterRes.output) !== "undefined") {
          output2 = afterRes.output;
        }
        return output2;
      };
      const result = withHooks(input);
      if (result instanceof Promise || (result == null ? void 0 : result.then) && (result == null ? void 0 : result.catch)) {
        return result.then((res) => {
          output = res;
          success = true;
          return res;
        }).catch(async (err) => {
          if (!handleSignals(err)) {
            success = false;
            error = err;
          } else {
            const afterRes = await (afterHook == null ? void 0 : afterHook({
              iteration,
              tool,
              input,
              output,
              controller,
              object,
              toolCallId
            }));
            if (typeof (afterRes == null ? void 0 : afterRes.output) !== "undefined") {
              output = afterRes.output;
            }
          }
          throw err;
        }).finally(() => {
          cancelSlowTool();
          traces.push({
            type: "tool_call",
            tool_call_id: toolCallId,
            started_at: toolStart,
            ended_at: Date.now(),
            tool_name: tool.name,
            object,
            input: getToolInput(input),
            output,
            error,
            success
          });
        });
      }
      success = true;
      output = result;
    } catch (err) {
      if (!handleSignals(err)) {
        success = false;
        error = err;
      }
    }
    cancelSlowTool();
    traces.push({
      type: "tool_call",
      tool_call_id: toolCallId,
      started_at: toolStart,
      ended_at: Date.now(),
      tool_name: tool.name,
      object,
      input: getToolInput(input),
      output,
      error,
      success
    });
    if (!success) {
      throw error;
    }
    if (output instanceof _chunkPK72FAKDcjs.VMSignal) {
      throw output;
    }
    return output;
  };
}
var executeOnIterationStartHook = async (props) => {
  const { iteration, ctx, onIterationStart, controller, onIterationEnd } = props;
  try {
    const hookRes = await (onIterationStart == null ? void 0 : onIterationStart(iteration, controller, ctx));
    if (hookRes) {
      Object.assign(iteration, hookRes);
    }
  } catch (err) {
    if (err instanceof _chunkPK72FAKDcjs.ThinkSignal) {
      iteration.end({
        type: "thinking_requested",
        thinking_requested: {
          variables: err.context,
          reason: err.reason
        }
      });
      try {
        await (onIterationEnd == null ? void 0 : onIterationEnd(iteration, controller));
      } catch (err2) {
        console.error(err2);
      }
    } else {
      iteration.end({
        type: "execution_error",
        execution_error: {
          message: `Error in onIterationStart hook: ${getErrorMessage(err)}`,
          stack: _chunkKIN7Y247cjs.cleanStackTrace.call(void 0, _nullishCoalesce(err.stack, () => ( "No stack trace available")))
        }
      });
    }
    throw err;
  }
};



exports._executeContext = _executeContext; exports.executeContext = executeContext;
