/**
 * Validates a tag name according to ADK rules:
 * - Only alphanumeric characters (A-Z, a-z, 0-9) and underscores
 * - Minimum 3 characters
 * - Must start with a letter
 */
export declare function validateTagName(name: string): {
    valid: boolean;
    error?: string;
};
/**
 * Validates all tag names in a tag definition object
 * Throws an error if any tag name is invalid
 */
export declare function validateTagDefinitions(tags: Record<string, {
    title: string;
    description?: string | undefined;
}>, context: string): void;
//# sourceMappingURL=validate-tag-name.d.ts.map