/**
 * Validates a secret name according to ADK rules:
 * - SCREAMING_SNAKE_CASE: only uppercase letters (A-Z), digits (0-9), and underscores
 * - Minimum 2 characters
 * - Must start with a letter
 * - Must not start with reserved prefixes (SECRET_, BP_, BOTPRESS_)
 */
export declare function validateSecretName(name: string): {
    valid: boolean;
    error?: string;
};
/**
 * Validates all secret names in a secret definition object.
 * Throws an error if any secret name is invalid.
 */
export declare function validateSecretDefinitions(secrets: Record<string, {
    optional?: boolean | undefined;
    description?: string | undefined;
}>, context: string): void;
//# sourceMappingURL=validate-secret-name.d.ts.map