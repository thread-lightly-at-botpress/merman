/**
 * Utility functions for matching conversation handlers to channels
 */
export type ConversationHandler = {
    channel: string | string[] | '*';
    [key: string]: any;
};
/**
 * Check if a handler's channel specification matches an incoming channel
 */
export declare function matchesChannel(handlerChannel: string | string[] | '*', incomingChannel: string): boolean;
/**
 * Find the most specific matching handler for a given channel
 * Prioritization: single channel > array > glob (*)
 */
export declare function findMatchingHandler<T extends ConversationHandler>(handlers: T[], incomingChannel: string): T | undefined;
//# sourceMappingURL=conversation-matching.d.ts.map